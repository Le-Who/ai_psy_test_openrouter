/**
 * AI Universal Test Generator - Configuration
 * ============================================
 * Этот файл содержит настройки моделей, системные промпты и схемы данных.
 * Подключается к index.html через тег <script src="config.js">.
 */

const CONFIG = {
    // ===== МОДЕЛИ =====
    // Выбор провайдера и модели для разных этапов генерации
    models: {
        // Архитектор (Blueprint) - определяет структуру теста (персонажи или шкалы)
        // Рекомендуется: GPT-4o-mini (баланс скорость/качество) или Claude-3.5-sonnet
        architect: 'openai/gpt-4o-mini',
        
        // Генератор контента (Questions) - создает сами вопросы
        // Рекомендуется: GPT-4o-mini
        generator: 'openai/gpt-4o-mini',
    },

    // ===== ПАРАМЕТРЫ ГЕНЕРАЦИИ =====
    generation: {
        // Temperature (креативность): 0.0 = детерминированно, 1.0 = максимально креативно
        temperature: 0.7,
        
        // Max tokens (максимальная длина ответа)
        maxTokens: 4000,
        
        // Top P (nucleus sampling)
        topP: 0.95,
        
        // Timeout для API запросов (миллисекунды)
        timeoutMs: 45000
    },

    // ===== ОГРАНИЧЕНИЯ =====
    limits: {
        minQuestions: 5,
        maxQuestions: 20,
        minOutcomes: 2,  // Минимум результатов (персонажей/шкал)
        maxOutcomes: 12  // Максимум результатов
    },

    // ===== UI НАСТРОЙКИ =====
    ui: {
        // Задержка перед переходом к следующему вопросу (мс)
        answerDelayMs: 250,
        
        // Показывать технические детали в консоли браузера (F12)
        debugMode: true,
        
        // Задержка анимации результатов
        resultsAnimationDelay: 600
    }
};

// ===== СИСТЕМНЫЕ ПРОМПТЫ =====
// Инструкции, которые определяют поведение AI на каждом этапе

const PROMPTS = {
    // Этап 1: Архитектор (определяет структуру теста)
    architect: `Ты — Архитектор Психометрических Систем.

Твоя задача — проанализировать запрос пользователя и спроектировать идеальную структуру теста.

ТИПЫ ТЕСТОВ:
1. **categorical** (Категориальный) — для вопросов вида:
   - "Кто ты из [Франшизы]?"
   - "Какой у тебя [Архетип]?"
   - "Твой тип личности по [Системе]"
   → Результат: определение одного победителя (персонажа/типа) из списка.

2. **dimensional** (Размерный) — для вопросов вида:
   - "Уровень твоей [Черты]"
   - "Оценка [Качества]"
   - "Насколько ты [Прилагательное]"
   → Результат: баллы по одной или нескольким независимым шкалам.

ТРЕБОВАНИЯ К OUTCOMES (Результатам):
- Для categorical: создай 4-10 персонажей/типов с уникальными характеристиками.
- Для dimensional: создай 1-5 психологических шкал (измерений).
- Имена и описания строго на РУССКОМ языке.
- Описания должны быть краткими, но емкими (2-3 предложения).

ВАЖНО:
- Если тема неоднозначна — выбирай более интересный, развлекательный вариант (обычно categorical).
- Избегай банальных формулировок.`,

    // Этап 2: Генератор (создает вопросы)
    generator: `Ты — Профессиональный Автор Тестов.

Твоя задача — создать интересные вопросы, которые распределяют людей по утвержденным результатам.

ПРАВИЛА ДЛЯ ВОПРОСОВ:
1. **Язык**: Строго РУССКИЙ.
2. **Формат**: Утверждения от первого лица ("Я люблю...", "Мне нравится..."), с которыми пользователь соглашается или нет.
3. **Длина**: 10-20 слов.
4. **Стиль**: Естественный, разговорный, подходящий теме.

ПРАВИЛА ДЛЯ MAPPING (Связь вопросов с результатами):

**Для CATEGORICAL (Персонажи/Типы):**
- Каждый вопрос должен быть индикатором одной или нескольких черт.
- Вес +1 (или больше): Ответ "Согласен" приближает к этому персонажу.
- Вес -1 (или меньше): Ответ "Согласен" отдаляет от этого персонажа (противоречит ему).
- Указывай mapping только для тех персонажей, на которых этот вопрос реально влияет.

**Для DIMENSIONAL (Шкалы):**
- Вес +1: Прямая корреляция (ответ "Да" повышает балл шкалы).
- Вес -1: Обратная корреляция (ответ "Да" понижает балл шкалы).

ЦЕЛЬ: Максимально точно определить результат пользователя.`
};

// ===== JSON SCHEMAS =====
// Схемы данных для Structured Outputs (гарантируют формат JSON)

const SCHEMAS = {
    // Схема 1: Blueprint (Структура теста)
    blueprint: {
        "type": "json_schema",
        "json_schema": {
            "name": "test_blueprint",
            "strict": true,
            "schema": {
                "type": "object",
                "properties": {
                    "testType": {
                        "type": "string",
                        "enum": ["dimensional", "categorical"],
                        "description": "dimensional = шкалы/качества, categorical = персонажи/типы"
                    },
                    "language": { "type": "string", "enum": ["ru"] },
                    "outcomes": {
                        "type": "array",
                        "description": "Список возможных результатов",
                        "items": {
                            "type": "object",
                            "properties": {
                                "id": { "type": "string" },
                                "name": { "type": "string", "description": "Имя результата на русском" },
                                "description": { "type": "string", "description": "Описание результата на русском" }
                            },
                            "required": ["id", "name", "description"],
                            "additionalProperties": false
                        }
                    }
                },
                "required": ["testType", "language", "outcomes"],
                "additionalProperties": false
            }
        }
    },

    // Схема 2: Questions (Пакет вопросов)
    questions: {
        "type": "json_schema",
        "json_schema": {
            "name": "test_questions_pack",
            "strict": true,
            "schema": {
                "type": "object",
                "properties": {
                    "questions": {
                        "type": "array",
                        "items": {
                            "type": "object",
                            "properties": {
                                "text": { "type": "string", "description": "Текст утверждения на русском языке" },
                                "mapping": {
                                    "type": "array",
                                    "description": "Связь вопроса с результатами (outcomeId + weight)",
                                    "items": {
                                        "type": "object",
                                        "properties": {
                                            "outcomeId": { "type": "string" },
                                            "weight": { 
                                                "type": "number", 
                                                "description": "Вес влияния: положительный (+1) или отрицательный (-1)" 
                                            }
                                        },
                                        "required": ["outcomeId", "weight"],
                                        "additionalProperties": false
                                    }
                                }
                            },
                            "required": ["text", "mapping"],
                            "additionalProperties": false
                        }
                    }
                },
                "required": ["questions"],
                "additionalProperties": false
            }
        }
    }
};

// Валидация загрузки (для отладки)
console.log("✅ Config.js loaded successfully");
